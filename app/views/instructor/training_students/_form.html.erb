<%= form_for @training_student, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :training_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :training_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :student_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :student_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                instructor_training_students_path, :class => 'btn' %>
  </div>
<% end %>
